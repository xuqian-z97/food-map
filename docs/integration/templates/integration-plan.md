# 联调说明模板

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 联调名称 |  |
| 迭代编号 |  |
| 联调文件夹 |  |
| 创建时间 |  |
| 主代理 |  |
| 后端观察子代理 |  |
| 前端观察子代理 |  |
| 联调验收子代理 |  |
| 前端提交 |  |
| 后端提交 |  |
| 文档提交 |  |
| 相关文档 |  |

## 2. 联调目标

本次联调需要验证的功能：

- [ ] 

本次不纳入联调范围的功能：

- [ ] 

## 3. 联调安全点

本次联调等级：

- [ ] L0 契约联调
- [ ] L1 Mock 联调
- [ ] L2 本地真实联调
- [ ] L3 环境联调
- [ ] L4 验收联调

准入检查：

| 检查项 | 前端状态 | 后端状态 | 结论 |
| --- | --- | --- | --- |
| API 契约已确认 |  |  | 未确认 |
| 前端可发起真实请求 |  | - | 未确认 |
| 后端接口可被 curl/Postman/测试调通 | - |  | 未确认 |
| 测试数据已准备 |  |  | 未确认 |
| `requestId` / `traceId` 可追踪 |  |  | 未确认 |
| 必要环境已启动 |  |  | 未确认 |
| 本次范围已冻结 |  |  | 未确认 |

联调开始前结论：

- [ ] 可以开始联调
- [ ] 暂不开始，前端需要补齐：
- [ ] 暂不开始，后端需要补齐：
- [ ] 暂不开始，环境或测试数据需要补齐：

## 4. 前端职责

| 页面/模块 | 交互 | 预期效果 | 状态要求 | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  | 加载中 / 成功 / 失败 / 空状态 / 无权限 / 网络不可用 | 截图、录屏、网络请求记录 |

前端必须说明：

- 页面入口和导航路径。
- 用户操作步骤。
- Token、用户身份和本地缓存处理方式。
- iOS 版本、模拟器或真机型号、Xcode 版本、App 构建号、后端 Base URL。
- 网络失败、登录失效、权限不足、参数错误时的展示方式。
- 不输出 Token、密码、私密推荐正文、评论正文或完整敏感信息。

## 5. 后端职责

| 服务 | 接口 | 方法 | 入参 | 响应 | 错误码 | 日志要求 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  | requestId / traceId / serviceName |

后端必须说明：

- 服务边界和数据库归属。
- 本地、OrbStack 或测试环境 profile、端口和必须启动的中间件。
- Controller 请求/响应 DTO。
- 关键参数校验规则。
- 认证身份来源和权限校验规则。
- 事务、幂等、分页、bbox 或缓存策略。
- 访问日志、业务日志、异常日志和必要审计日志。
- 是否涉及写接口、跨服务调用、PUBLIC 统计、对象存储、Kafka/Elasticsearch 或日志平台真实联调。

## 6. API 契约

### 6.1 请求

```http
METHOD /path
Content-Type: application/json
Authorization: Bearer <access-token>
X-Request-Id: <request-id>
```

```json
{}
```

### 6.2 成功响应

```json
{
  "success": true,
  "status": 200,
  "code": "SUCCESS",
  "message": "success",
  "data": {}
}
```

### 6.3 失败响应

| 场景 | HTTP status | code | message 要求 |
| --- | --- | --- | --- |
| 未认证或登录失效 | 401 |  | 不暴露内部异常 |
| 权限不足 | 403 |  | 不混用 401 |
| 参数错误 | 400 |  | 指向可修正字段 |
| 服务异常 | 500 |  | 不暴露堆栈、SQL、Token |

## 7. 测试数据

| 数据类型 | 数据值 | 创建方式 | 使用场景 | 清理方式 |
| --- | --- | --- | --- | --- |
| 账号 |  |  |  |  |
| Token | 只记录是否存在，不记录明文 |  |  |  |
| 门店/推荐/关系数据 |  |  |  |  |

## 8. 联调环境

| 组件 | 地址/配置 | 状态 |
| --- | --- | --- |
| iOS App |  |  |
| iOS 版本 / 设备 |  |  |
| Xcode / App 构建号 |  |  |
| Gateway |  |  |
| 后端服务 |  |  |
| PostgreSQL |  |  |
| Redis |  |  |
| Nacos |  |  |
| Kafka/Elasticsearch/日志服务 |  |  |

## 9. 证据附件

可选附件目录：

```text
screenshots/
recordings/
network/
logs/
tests/
```

证据要求：

- 前端截图或录屏应覆盖关键成功态和失败态。
- 网络证据只保留脱敏摘要，包括 method、path、status、业务 code、message、latency、requestId、traceId。
- 日志证据只保留脱敏摘要，不保存 Token、密码、完整手机号、完整邮箱、私密正文或评论正文。

## 10. 验收场景

| 编号 | 场景 | 操作步骤 | 预期结果 | 通过情况 | 证据 |
| --- | --- | --- | --- | --- | --- |
| IT-001 |  |  |  | 未执行 |  |

## 11. 日志和排查要求

每个失败场景至少记录：

- `requestId`
- `traceId`
- 前端网络请求摘要
- 后端服务名和接口路径
- 后端日志摘要
- 数据库或中间件状态摘要，如适用

禁止记录：

- Access Token / Refresh Token 明文
- 密码、密钥
- 完整手机号、完整邮箱
- 私密推荐正文、评论正文
- SQL 明文敏感参数

## 12. 验收判定口径

- `通过`：必测场景全部执行并有证据，阻塞级和高风险 BUG 已关闭。
- `有条件通过`：主流程可用，仅存在明确记录的非阻塞问题，并有责任人和复测计划。
- `不通过`：主流程失败，或出现权限、隐私、Token、可见范围、PUBLIC 统计口径等高风险问题。
- `无法判定`：缺少环境、构建、测试、接口调用、截图/录屏、日志等关键证据。

## 13. 联调结论

| 项目 | 结论 |
| --- | --- |
| 是否通过 | 未开始 |
| 阻塞问题 |  |
| 高风险问题 |  |
| 已执行验证 |  |
| 未执行验证及原因 |  |
| 后续动作 |  |
