server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: foodmap-recommendation-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}

management:
  endpoints:
    web:
      exposure:
        include: health,info
