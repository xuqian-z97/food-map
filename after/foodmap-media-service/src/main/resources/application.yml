server:
  port: ${SERVER_PORT:8087}

spring:
  application:
    name: foodmap-media-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}

management:
  endpoints:
    web:
      exposure:
        include: health,info
