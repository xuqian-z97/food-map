spring:
  datasource:
    url: ${MEDIA_DB_URL:jdbc:postgresql://127.0.0.1:${POSTGRES_PORT:5432}/${MEDIA_DB_NAME:foodmap_media_db}}
    username: ${MEDIA_DB_USERNAME:${POSTGRES_USER:foodmap}}
    password: ${MEDIA_DB_PASSWORD:${POSTGRES_PASSWORD:foodmap_dev_pwd}}
    hikari:
      pool-name: ${MEDIA_DB_POOL_NAME:foodmap-media-hikari}
      maximum-pool-size: ${MEDIA_DB_POOL_MAX_SIZE:${DB_POOL_MAX_SIZE:5}}
      minimum-idle: ${MEDIA_DB_POOL_MIN_IDLE:${DB_POOL_MIN_IDLE:1}}
      connection-timeout: ${MEDIA_DB_POOL_CONNECTION_TIMEOUT_MS:${DB_POOL_CONNECTION_TIMEOUT_MS:3000}}
      idle-timeout: ${MEDIA_DB_POOL_IDLE_TIMEOUT_MS:${DB_POOL_IDLE_TIMEOUT_MS:600000}}
      max-lifetime: ${MEDIA_DB_POOL_MAX_LIFETIME_MS:${DB_POOL_MAX_LIFETIME_MS:1800000}}
      leak-detection-threshold: ${MEDIA_DB_POOL_LEAK_DETECTION_MS:${DB_POOL_LEAK_DETECTION_MS:10000}}
  flyway:
    enabled: ${MEDIA_FLYWAY_ENABLED:true}
    locations: classpath:db/migration
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}

foodmap:
  logging:
    sql:
      enabled: true
      debug-enabled: true
      sample-rate: 1.0
      mapper-includes:
        - "*"
      mapper-excludes: []

logging:
  level:
    com.foodmap.common.logging.mybatis: DEBUG
